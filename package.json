{
  "name": "cash-flow",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "lite-server --baseDir='dist'",
    "start": "concurrently \"npm run sass\" \"npm run watch\" \"npm run dev\"",
    "compile": "tsc",
    "sass": "sass --watch src/style/main.sass dist/style/main.css",
    "watch": "tsc -w"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lite-server": "^2.6.1",
    "typescript": "^4.4.3"
  },
  "dependencies": {
    "concurrently": "^6.2.1"
  }
}
